---
description: Pre-merge Definition of Done checklist before commit/PR
---

Follow the project skill **dn-dod** (`.claude/skills/dn-dod/SKILL.md`) exactly.

Walk the full checklist against the current working tree and recent verification evidence.
Run missing tests/smoke when required. Only proceed to commit if every item passes or is explicitly waived (docs-only).

If `$ARGUMENTS` contains a commit intent or ID, use it when drafting the message after the checklist passes.
