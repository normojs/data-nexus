---
description: True-streaming / passthrough / result-path workflow (A06–A10)
---

Follow the project skill **dn-stream** (`.claude/skills/dn-stream/SKILL.md`) exactly.

Force-read `.claude/rules/streaming-performance.md` and `todo.md` A06–A10 + §3.6.

If `$ARGUMENTS` names a focus (e.g. A09 portal), implement that slice; otherwise use `todo.md` §4 streaming focus.

Do not advertise HTTP chunk or partial paths as end-to-end streaming. Mark partial work honestly in `todo.md`.
