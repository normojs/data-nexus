---
description: True-streaming / passthrough / result-path workflow (A06–A10)
---

Follow the project skill **dn-stream** (`.claude/skills/dn-stream/SKILL.md`) exactly.

Force-read `.claude/rules/streaming-performance.md` and `todo.md` A06–A10 (§1) + §4 honesty.

If `$ARGUMENTS` names a focus (e.g. A09 portal), implement that slice; otherwise use `todo.md` §5 streaming focus.

Do not advertise HTTP chunk or partial paths as end-to-end streaming. Keep partial work as `- [ ]` with updated 已有/仍欠 in `todo.md`.
