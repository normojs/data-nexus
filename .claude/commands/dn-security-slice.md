---
description: End-to-end security/policy slice delivery (F*/B*/S*/H*/UI*)
---

Follow the project skill **dn-security-slice** (`.claude/skills/dn-security-slice/SKILL.md`) exactly.

Focus ID: `$ARGUMENTS` if provided; otherwise ask via **dn-board** / `todo.md` §5.

Enforce iron laws: portal via PEP, admin ≠ data subject, audit non-blocking, no silent config no-op, `security.enabled=false` preserves v1.
