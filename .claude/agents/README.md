# Agents

| Agent | 用途 |
|-------|------|
| [`dn-reviewer.md`](dn-reviewer.md) | 对照铁律 / 流式诚实账 / smoke 纪律做 diff 评审 |

日常开发优先用 **Skills** 与 **Commands**（`/dn-board` `/dn-stream` `/dn-smoke` `/dn-dod`）。仅在用户明确要求评审或需要独立视角时 spawn `dn-reviewer`。
